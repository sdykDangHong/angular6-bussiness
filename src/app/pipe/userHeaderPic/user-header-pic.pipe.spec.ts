import { UserHeaderPicPipe } from './user-header-pic.pipe';

describe('UserHeaderPicPipe', () => {
  it('create an instance', () => {
    const pipe = new UserHeaderPicPipe();
    expect(pipe).toBeTruthy();
  });
});
