import { FreelancerSkillFilterPipe } from './freelancer-skill-filter.pipe';

describe('FreelancerSkillFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FreelancerSkillFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
