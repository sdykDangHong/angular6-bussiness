import { DemandStatusPipe } from './demand-status.pipe';

describe('DemandStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DemandStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
